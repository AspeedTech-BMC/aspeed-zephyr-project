/ {
	chosen {
		zephyr,console = &uart12;
		zephyr,shell-uart = &uart12;
	};

	reserved-memory {
		#address-cells = <1>;
		#size-cells = <1>;
		ranges;

		ssp_memory: ssp-memory@f0100000 {
			reg = <0xf0100000 0x2000000>;
			no-map;
			bootph-all;
		};
	};
};
